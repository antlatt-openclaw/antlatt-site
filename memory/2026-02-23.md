# 2026-02-23 Memory

## Website Redesign - Complete

### Major Milestone
Fully migrated antlatt.com from old static HTML (Hielo template) to modern Astro + Tailwind stack.

### Self-Hosted Setup
- Docker container `antlatt-site` running on 192.168.1.205:8080
- nginx serves static site + 30 deepfake videos (~11GB)
- Videos accessible at `/videos/` endpoint
- Thumbnails generated via ffmpeg for all videos
- Ready for antlatt.com DNS (point to Nginx Proxy Manager → 192.168.1.205:8080)

### Features Implemented (10 upgrades via subagent)
1. Client-side search with static index
2. Live infrastructure dashboard (/lab) - IPs hidden for security
3. Table of Contents for long articles
4. Build cost calculator (interactive)
5. Parts tables with affiliate link support
6. RSS feed (/rss.xml)
7. Video embeds with thumbnails
8. Related builds section (tag-based)
9. Open Graph images (static default + dynamic per-article)
10. Callout boxes (lesson/tip/warning/info)

### Issues Fixed
- CSS not loading → global.css wasn't imported in BaseLayout
- Double slashes in URLs → fixed Astro.site handling
- Search API required backend → converted to client-side with static index
- Internal IPs exposed on /lab → replaced with generic names

### Vercel Backup
- Still deployed at https://antlatt-site.vercel.app
- Can be deleted or kept as backup

### Key Files
- Project: `/root/.openclaw/antlatt-workspace/website-redesign/antlatt-site/`
- Styles: `src/styles/global.css`
- Layout: `src/layouts/BaseLayout.astro`
- Builds: `src/content/builds/*.mdx`

---

## Notes for Future
- Website styling uses Tailwind v4 with `@tailwindcss/vite` plugin
- Color theme: dark bg `#0a0a0f`, accent `#6366f1` (indigo), code `#22d3ee` (cyan)
- All build articles have: reading time, TOC, callouts, related builds
- Deepfake videos are self-hosted, not on YouTube or external CDN